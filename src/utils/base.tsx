import React from "react";
import styled from "styled-components";

export const Blog = () => {
  return (
    <BlogContainer>
      <p>hi</p>
    </BlogContainer>
  );
};

const BlogContainer = styled.div``;
